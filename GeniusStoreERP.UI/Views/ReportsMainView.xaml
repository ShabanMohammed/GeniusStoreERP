<UserControl x:Class="GeniusStoreERP.UI.Views.ReportsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeniusStoreERP.UI.Views"
             xmlns:vm="clr-namespace:GeniusStoreERP.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="مركز التقارير" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource MainContentTextBrush}"/>
                <TextBlock Text="استعرض التقارير المالية والتشغيلية للنظام مقسمة حسب النوع." 
                           Foreground="#7986CB" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Reports Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" VerticalAlignment="Top"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" 
                                Background="White" 
                                CornerRadius="12" 
                                Padding="20"
                                Effect="{StaticResource UnifiedCardStyle_ShadowOnly}"
                                VerticalAlignment="Top">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Icon & Category Title -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Border Width="40" Height="40" 
                                            Background="#EEF2FF" 
                                            CornerRadius="10">
                                        <Viewbox Width="20" Height="20">
                                            <Path Data="{Binding IconKey, Converter={StaticResource ResourceKeyToDataConverter}}" 
                                                  Fill="{StaticResource AccentActionBrush}" 
                                                  Stretch="Uniform"/>
                                        </Viewbox>
                                    </Border>
                                    <TextBlock Text="{Binding Title}" 
                                               FontSize="18" FontWeight="Bold" 
                                               VerticalAlignment="Center" 
                                               Margin="15,0,0,0"
                                               Foreground="{StaticResource MainContentTextBrush}"/>
                                </StackPanel>

                                <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                           Foreground="#8C9EFF" FontSize="11" 
                                           TextWrapping="Wrap" Margin="0,0,0,15"/>

                                <Separator Grid.Row="2" Background="#F1F3F9" Margin="0,0,0,15"/>

                                <!-- Reports List -->
                                <ItemsControl Grid.Row="3" ItemsSource="{Binding Reports}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource UnifiedSecondaryButtonStyle}"
                                                    Margin="0,0,0,8" 
                                                    HorizontalContentAlignment="Stretch"
                                                    Command="{Binding OpenCommand}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding Description}" FontSize="10" Opacity="0.7"/>
                                                    </StackPanel>
                                                    <Viewbox Grid.Column="1" Width="12" Height="12" Margin="10,0,0,0">
                                                        <Path Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" 
                                                              Fill="{StaticResource MainContentTextBrush}"/>
                                                    </Viewbox>
                                                </Grid>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
