<UserControl x:Class="GeniusStoreERP.UI.Views.Stock.StockAdjustmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="تسويات المخزن" Style="{StaticResource MainHeaderStyle}" FontSize="{StaticResource FontSizeLarge}"/>
                <TextBlock Text="إدارة وتسجيل حركات تسوية كميات الأصناف بالمستودع" Style="{StaticResource SubHeaderStyle}" FontSize="{StaticResource FontSizeNormal}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <!-- Search Box -->
                <Border Style="{StaticResource UnifiedInputContainerStyle}" Width="250" Margin="0,0,15,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Viewbox Width="16" Height="16" Margin="10,0">
                            <Path Data="{StaticResource IconActivity}" Fill="#94A3B8" Stretch="Uniform"/>
                        </Viewbox>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0" 
                                 Background="Transparent" 
                                 VerticalContentAlignment="Center"
                                 Tag="بحث برقم التسوية أو الملاحظات..."/>
                    </Grid>
                </Border>

                <Button Command="{Binding AddCommand}" Style="{StaticResource UnifiedPrimaryButtonStyle}" Width="160">
                    <StackPanel Orientation="Horizontal">
                                <Viewbox Width="16" Height="16" Margin="0,0,8,0">
                                    <Path Data="{StaticResource IconAdd}" Fill="White" Stretch="Uniform"/>
                                </Viewbox>
                        <TextBlock Text="إضافة تسوية جديدة"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content Area -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Effect="{StaticResource UnifiedCardStyle_ShadowOnly}">
            <Grid>
                <DataGrid ItemsSource="{Binding Adjustments}" AutoGenerateColumns="False" IsReadOnly="True" SelectedItem="{Binding SelectedAdjustment}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم التسوية" Binding="{Binding ReferenceNumber}" Width="140"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding AdjustmentDate, StringFormat=\{0:yyyy/MM/dd HH:mm\}}" Width="150"/>
                        <DataGridTextColumn Header="عدد الأصناف" Binding="{Binding TotalProducts}" Width="100"/>
                        <DataGridTextColumn Header="الملاحظات" Binding="{Binding Remarks}" Width="*"/>
                        <DataGridTemplateColumn Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource UnifiedSecondaryButtonStyle}" 
                                            Content="تفاصيل" Height="30" Padding="10,0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
                <ProgressBar IsIndeterminate="True" VerticalAlignment="Top" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Footer / Pagination -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#64748B" FontSize="{StaticResource FontSizeSmall}">
                    <Run Text="إجمالي السجلات: "/>
                    <Run Text="{Binding TotalItems}" FontWeight="Bold"/>
                </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource UnifiedSecondaryButtonStyle}" Width="40" Margin="0,0,10,0">
                    <Viewbox Width="16" Height="16"><Path Data="{StaticResource IconPreviousPage}" Fill="#1E293B" Stretch="Uniform"/></Viewbox>
                </Button>
                <Border Background="#F1F5F9" CornerRadius="8" Padding="15,0" Margin="0,0,10,0">
                    <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" FontSize="{StaticResource FontSizeSmall}">
                        <Run Text="صفحة "/><Run Text="{Binding CurrentPage}"/><Run Text=" من "/><Run Text="{Binding TotalPages}"/>
                    </TextBlock>
                </Border>
                <Button Command="{Binding NextPageCommand}" Style="{StaticResource UnifiedSecondaryButtonStyle}" Width="40">
                    <Viewbox Width="16" Height="16"><Path Data="{StaticResource IconNextPage}" Fill="#1E293B" Stretch="Uniform"/></Viewbox>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
