<UserControl x:Class="GeniusStoreERP.UI.Views.CategoryEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FlowDirection="RightToLeft">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Style="{StaticResource UnifiedCardStyle}" Width="500">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource MainHeaderStyle}" Margin="0,0,0,30"/>

                <!-- Name Field -->
                <TextBlock Text="اسم التصنيف" Style="{StaticResource FieldLabelStyle}"/>
                <Border Style="{StaticResource UnifiedInputContainerStyle}">
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UnifiedTextBoxStyle}"/>
                </Border>

                <!-- Description Field -->
                <TextBlock Text="الوصف" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,8"/>
                <Border Style="{StaticResource UnifiedInputContainerStyle}" Height="120">
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource UnifiedTextBoxStyle}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalContentAlignment="Top"/>
                </Border>

                <!-- Action Buttons -->
                <Grid Margin="0,30,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Command="{Binding SaveCommand}" 
                            Content="حفظ التغييرات" 
                            Style="{StaticResource UnifiedPrimaryButtonStyle}"
                            Grid.Column="0"
                            Margin="0,0,10,0"/>

                    <Button Command="{Binding CancelCommand}" 
                            Content="إلغاء" 
                            Background="#F5F6F7"
                            Foreground="#7F8C8D"
                            Style="{StaticResource UnifiedPrimaryButtonStyle}"
                            Grid.Column="1"
                            Margin="10,0,0,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
