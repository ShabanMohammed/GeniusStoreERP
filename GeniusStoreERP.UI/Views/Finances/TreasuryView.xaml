<UserControl x:Class="GeniusStoreERP.UI.Views.Finances.TreasuryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GeniusStoreERP.UI.ViewModels.Finances"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1000"
             FlowDirection="RightToLeft">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding PageTitle}" Style="{StaticResource MainHeaderStyle}" Grid.Column="0" VerticalAlignment="Center"/>
            <Button Command="{Binding AddTreasuryCommand}" Style="{StaticResource UnifiedPrimaryButtonStyle}" Grid.Column="1" Width="150" ToolTip="إضافة خزينة جديدة">
                <StackPanel Orientation="Horizontal">
                    <Viewbox Width="16" Height="16" Margin="0,0,8,0">
                        <Path Data="{StaticResource IconAdd}" Fill="White" Stretch="Uniform"/>
                    </Viewbox>
                    <TextBlock Text="إضافة خزينة" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Treasuries List (Left) -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,20,0" Effect="{StaticResource UnifiedCardStyle_ShadowOnly}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#F8F9FA" Padding="15" CornerRadius="12,12,0,0">
                        <TextBlock Text="الخزائن المتاحة" FontWeight="Bold" Foreground="#2C3E50"/>
                    </Border>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Treasuries}" SelectedItem="{Binding SelectedTreasury}" BorderThickness="0" Background="Transparent" Padding="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10" Margin="5" Background="#F1F3F5" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                            <TextBlock Text="{Binding Code}" FontSize="12" Foreground="#7F8C8D"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding Balance, StringFormat=\{0:N2\}}" VerticalAlignment="Center" FontWeight="Bold" Foreground="#27AE60"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Transactions Grid (Right) -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" Effect="{StaticResource UnifiedCardStyle_ShadowOnly}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Background="#F8F9FA" Padding="15" CornerRadius="12,12,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="حركات الخزينة: " FontWeight="Bold" Foreground="#2C3E50"/>
                                <TextBlock Text="{Binding SelectedTreasury.Name}" FontWeight="Bold" Foreground="#3498DB"/>
                            </StackPanel>
                            <Button Grid.Column="1" Command="{Binding AddTransactionCommand}" Style="{StaticResource UnifiedPrimaryButtonStyle}" Background="#27AE60" BorderBrush="#27AE60" Width="130">
                                <StackPanel Orientation="Horizontal">
                                    <Viewbox Width="14" Height="14" Margin="0,0,5,0">
                                        <Path Data="{StaticResource IconExchange}" Fill="White" Stretch="Uniform"/>
                                    </Viewbox>
                                    <TextBlock Text="إضافة حركة" FontSize="12"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding TransactionDate, StringFormat=\{0:yyyy/MM/dd HH:mm\}}" Width="140"/>
                            <DataGridTemplateColumn Header="النوع" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="5,2" CornerRadius="10" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Type}" Value="CashIn">
                                                            <Setter Property="Background" Value="#E8F5E9"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Type}" Value="CashOut">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Type}" Value="CashIn">
                                                                <Setter Property="Text" Value="وارد / قبض"/>
                                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="CashOut">
                                                                <Setter Property="Text" Value="صادر / صرف"/>
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat=\{0:N2\}}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="الشريك" Binding="{Binding PartnerName}" Width="150"/>
                            <DataGridTextColumn Header="رقم المرجع" Binding="{Binding ReferenceNumber}" Width="100"/>
                            <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
