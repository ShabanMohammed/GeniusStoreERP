<UserControl x:Class="GeniusStoreERP.UI.Views.Users.UserManagementEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GeniusStoreERP.UI.ViewModels.Users"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1000"
             FlowDirection="RightToLeft">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding CancelCommand}" Style="{StaticResource UnifiedIconButtonStyle}" Margin="0,0,20,0">
                    <Viewbox Width="20" Height="20">
                        <Path Data="{StaticResource IconPreviousPage}" Fill="{StaticResource MainContentTextBrush}" Stretch="Uniform"/>
                    </Viewbox>
                </Button>
                <TextBlock FontSize="28" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource MainHeaderStyle}">
                            <Setter Property="Text" Value="إضافة مستخدم جديد"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Text" Value="تعديل بيانات المستخدم"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Editor Card -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="White" CornerRadius="16" MaxWidth="600" HorizontalAlignment="Left" Padding="40" Effect="{StaticResource UnifiedCardStyle_ShadowOnly}">
                <StackPanel>
                    
                    <TextBlock Text="البيانات الأساسية" FontSize="20" FontWeight="Bold" Margin="0,0,0,25" Foreground="{StaticResource AccentActionBrush}"/>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="الاسم الكامل" Style="{StaticResource FieldLabelStyle}"/>
                        <Border Style="{StaticResource UnifiedInputContainerStyle}">
                            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UnifiedTextBoxStyle}" Tag="أدخل الاسم بالكامل..."/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="اسم المستخدم" Style="{StaticResource FieldLabelStyle}"/>
                        <Border Style="{StaticResource UnifiedInputContainerStyle}">
                            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UnifiedTextBoxStyle}" Tag="اسم تسجيل الدخول..." IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"/>
                        </Border>
                        <TextBlock Text="* لا يمكن تغيير اسم المستخدم بعد الإنشاء" FontSize="11" Foreground="#E74C3C" Margin="10,5,0,0" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="البريد الإلكتروني" Style="{StaticResource FieldLabelStyle}"/>
                        <Border Style="{StaticResource UnifiedInputContainerStyle}">
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UnifiedTextBoxStyle}" Tag="example@domain.com"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,20" Visibility="{Binding IsEditMode, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <TextBlock Text="كلمة المرور" Style="{StaticResource FieldLabelStyle}"/>
                        <Border Style="{StaticResource UnifiedInputContainerStyle}">
                            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource UnifiedTextBoxStyle}" Tag="أدخل كلمة المرور..."/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="صلاحية المستخدم (الدور)" Style="{StaticResource FieldLabelStyle}"/>
                        <Border Style="{StaticResource UnifiedInputContainerStyle}">
                            <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding Role}" Style="{StaticResource UnifiedComboBoxStyle}"/>
                        </Border>
                    </StackPanel>

                    <Separator Margin="0,0,0,30" Background="#F1F3F5"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Command="{Binding SaveCommand}" Style="{StaticResource UnifiedPrimaryButtonStyle}" Content="حفظ البيانات" Margin="0,0,10,0" Height="45" FontSize="16"/>
                        <Button Grid.Column="1" Command="{Binding CancelCommand}" Style="{StaticResource UnifiedSecondaryButtonStyle}" Content="إلغاء" Margin="10,0,0,0" Height="45" FontSize="16"/>
                    </Grid>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
